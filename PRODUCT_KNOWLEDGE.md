# PRODUCT KNOWLEDGE — سامانه ترخیصان (پنل‌های `/TSDashboard`، `/TSCards`، `/TSCardUser`)

> این سند صرفاً از روی **Implementation واقعی** پروژه استخراج شده است (کد React در `src/pages`، `src/components/admin`، و endpointهای PHP در `public/api`). هیچ قابلیتی حدس یا اضافه نشده؛ مواردی که از روی کد قابل تأیید کامل نبودند با برچسب **UNVERIFIED** مشخص شده‌اند.

---

## ۱. نمای کلی محصول

سامانه‌ای برای مدیریت «کارت‌های بازرگانی/سهمیه ارزی» و ثبت کوتاژهای گمرکی روی آن سهمیه‌هاست. سه پنل مستقل وجود دارد:

| مسیر | مخاطب | نقش |
|---|---|---|
| `/TSDashboard` | ادمین | داشبورد اصلی: سرنخ‌ها (Leads)، مشاوره کارت بازرگانی، تنظیمات پایه (کدهای گمرکی، دسته‌بندی سکشن‌ها) |
| `/TSCards` | ادمین | هستهٔ عملیاتی: کارت‌ها، کاربران کارت، پرداخت‌ها، کوتاژها، بانک ترخیصان (خزانه)، گزارش‌گیری، بک‌آپ |
| `/TSCardUser` | کاربر کارت (مشتری) | مشاهدهٔ سهمیه‌های تخصیص‌یافته، ثبت/ویرایش کوتاژ، ثبت پرداخت، صورتحساب |

دو سیستم احراز هویت **کاملاً مجزا و متعامد**:
- ادمین: جدول `ts_admins` + نشست ادمین (`ts_admin_require()`)
- کاربر کارت: جدول `ts_card_users` + نشست کاربر کارت (`ts_carduser_require()`)

کوکی‌ها، جداول نشست و endpointها جدا هستند؛ هیچ‌کدام به منابع دیگری دسترسی ندارد. محافظت مسیرها در سطح **API** اعمال می‌شود (نه صرفاً UI).

---

## ۲. مدل داده (Data Model)

| جدول | نقش |
|---|---|
| `ts_admins` | حساب‌های ادمین |
| `ts_leads` | سرنخ‌های فرم‌های سایت + لاگ جستجوی HS Code |
| `ts_cards` | کارت‌ها؛ ستون‌های کلیدی: `name`, `balance`, `tolerance_percent`, `display_balance_usd`, `display_balance_irt`, `finalized_at` |
| `ts_card_entries` | «سکشن»های هر کارت: `title`, `amount`, `currency (USD/EUR/IRT)`, `unit_price_irt`, `sort_order` |
| `ts_card_users` | کاربران کارت: `first_name`, `last_name`, `username`, `password_hash`, `require_payment_approval` |
| `ts_card_user_access` | تخصیص کاربر ↔ سکشن کارت: `card_id`, `entry_id` (nullable)، `allocated`, `custom_unit_price_irt` |
| `ts_kotaj` | سربرگ کوتاژ: `kotaj_number`, `kotaj_date_jalali`, `kotaj_date_gregorian`, `entry_id`, `total_value_usd`, `attachments (JSON)` |
| `ts_kotaj_items` | اقلام کوتاژ: `name`, `value_usd`, `unit_price_irt` (اسنپ‌شات نرخ در لحظه ثبت) |
| `ts_card_payments` | پرداخت کاربر کارت به شرکت: `amount_irt`, `status (pending/confirmed/rejected)`, `to_treasury`, `pay_date_jalali/gregorian`, `receipt_paths` |
| `ts_card_admin_payments` | پرداخت ادمین به صاحب کارت: `amount_irt`, `status`, `from_treasury`, رسیدها |
| `ts_treasury_ledger` | دفتر کل «بانک ترخیصان»: `direction (in/out)`, `source ('user_payment' | 'admin_payment' | 'manual_adjust')`, `source_id`, `amount`, `occurred_at` |
| `ts_card_alloc_log` | تاریخچه تغییرات کارت/تخصیص: `create/update/delete/card_balance/card_delete` |
| `ts_customs_codes` | کدهای ۵ رقمی گمرک و نام آن‌ها |
| `ts_section_categories` | لیست آمادهٔ عناوین سکشن برای فرم کارت |
| `ts_backups` | متادیتای فایل‌های بک‌آپ ذخیره‌شده |

**الگوی معماری:** مهاجرت‌های Lazy/Self-healing — بسیاری از endpointها با `ts_column_exists()` بررسی می‌کنند ستون جدید (مثل `tolerance_percent`، `require_payment_approval`، `pay_date_*`) وجود دارد یا نه و در صورت نبود آن را می‌سازند یا نادیده می‌گیرند. این باعث سازگاری با نصب‌های قدیمی می‌شود.

---

## ۳. `/TSDashboard` — داشبورد ادمین

### ۳.۱ احراز هویت (ماشین حالت)
`loading → setup | login → auth`
- `setup`: وقتی هیچ ادمینی در `ts_admins` وجود ندارد → فرم ساخت اولین ادمین.
- `login`: فرم ورود ادمین.
- `auth`: نمایش پنل.
- نشست کوکی‌محور در `db.php`؛ `ts_cors_same_origin()` در ابتدای هر endpoint.

### ۳.۲ `LeadsPanel`
- لیست سرنخ‌ها با فیلتر وضعیت و جستجو، شامل لاگ جستجوهای HS Code.
- **UNVERIFIED/نکته:** در `leads.php` فیلتر `status` سمت کلاینت اعمال می‌شود در حالی که واکشی محدود است؛ در حجم بالا ممکن است نتایج ناقص نمایش داده شود.

### ۳.۳ `CardConsultPanel`
مدیریت درخواست‌های مشاورهٔ اخذ کارت بازرگانی ثبت‌شده از سایت.

### ۳.۴ `SettingsPanel`
- **کدهای گمرکی** (`CustomsCodesSettings`): CRUD روی `ts_customs_codes`؛ کد باید دقیقاً `^\d{5}$`. پس از هر تغییر، کش سمت کلاینت با `hydrateCustomsCodes(true)` رفرش می‌شود تا فرم کوتاژ گمرک خروجی را درست تشخیص دهد.
- **دسته‌بندی سکشن‌ها** (`SectionCategoriesSettings`): CRUD روی `ts_section_categories`؛ حذف یک دسته روی کارت‌ها/کوتاژهای ثبت‌شده اثری ندارد (فقط از لیست انتخاب حذف می‌شود).

---

## ۴. `/TSCards` — پنل مدیریت کارت‌ها

هشت تب: کارت‌ها، پرداختی‌های کارت، مدیریت کاربران، پرداخت‌های کاربران، کوتاژها، بانک ترخیصان، گزارش‌گیری، بک‌آپ.
هدر شامل زنگ اعلان پرداخت‌های در انتظار (Poll هر ۶۰ ثانیه از `payments-pending-count.php`) و بج قرمز روی تب «پرداخت‌های کاربران».
در حالت خروج از نشست، این صفحه فرم لاگین ندارد و فقط به `/TSDashboard` لینک می‌دهد.

### ۴.۱ تب «کارت‌ها»
ستون‌ها: نام کارت، موجودی کل (دلار)، موجودی کل (تومان)، سکشن‌ها، کاربران (دسکتاپ)، عملیات. بدون صفحه‌بندی.

**نمایش موجودی:**
```
usdTotal = Σ entries[currency=USD].amount
اگر display_balance_usd ست شده باشد → نمایش آن + سطر کوچک «سقف: usdTotal»
وگرنه اگر tolerance_percent > 0 → سطر کوچک «با تلورانس: usdTotal × (1 + tol/100)»
(همین منطق برای تومان با display_balance_irt در برابر balance)
```

**وضعیت مانده (نام قبلی: وضعیت ادمین):**
```
effectiveBal = display_balance_irt ?? balance
diff = admin_paid_irt − effectiveBal
|diff| ≤ 0.0001 → «تسویه» (خاکستری)
diff > 0.0001  → «بدهکار» (قرمز)
diff < 0       → «بستانکار» (سبز)
```

**عملیات هر کارت:** گزارش کوتاژ، قیمت دلار (اختصاصی هر کاربر)، تاریخچه، هزینه، ویرایش، حذف، پرداخت بدهی، صورتحساب، اتمام/لغو اتمام.

**قانون «اتمام کارت» (`card-finalize.php`):**
- `finalize`: `display_balance_usd = Σ ts_kotaj.total_value_usd` و `display_balance_irt = Σ(item.value_usd × item.unit_price_irt)` و `finalized_at = NOW()`. سقف اصلی (`balance` و سکشن‌ها) دست‌نخورده می‌ماند و فقط کم‌رنگ زیر عدد نمایش داده می‌شود. لاگ با `action='card_balance'`.
- `reset`: بازگرداندن ستون‌های نمایشی به `NULL`.
- اثر: موجودی نمایشی، وضعیت مانده و دیالوگ پرداخت همگی بر پایهٔ مقدار نمایشی جدید محاسبه می‌شوند.

### ۴.۲ ساخت/ویرایش کارت (`CardDialog` — ویزارد ۲ مرحله‌ای)
**مرحله ۱:** نام کارت (الزامی)، درصد تلورانس، و سکشن‌های مبلغ (حداقل یک ردیف): عنوان (از لیست `ts_section_categories`)، مبلغ، ارز (`USD/EUR/IRT`)، قیمت هر واحد (برای IRT قفل و برابر ۱). مجموع دلاری و تومانی به‌صورت زنده و همراه معادل با تلورانس نمایش داده می‌شود.

**مرحله ۲:** تخصیص کاربران به هر سکشن؛ هر سکشن map مستقل `{userId: allocated}` دارد و در ذخیره به‌صورت `{entry_index, id, allocated}` ارسال و در بک‌اند به `entry_id` واقعی نگاشت می‌شود.

**قانون کلیدی (رفع تداخل سکشن‌های هم‌نام):** تطبیق سکشن‌ها بر پایهٔ **شناسهٔ یکتای entry** انجام می‌شود، نه عنوان؛ بنابراین چند سکشن با عنوان یکسان (مثلاً دو «تره‌بار») در یک کارت مستقل از هم باقی می‌مانند و مبلغ/نرخ یکدیگر را بازنویسی نمی‌کنند.

**اعتبارسنجی:** مجموع تخصیص‌های هر سکشن نباید از مبلغ آن بیشتر شود (آستانه `0.0001`)؛ در غیر این‌صورت دکمهٔ ثبت غیرفعال می‌شود.

### ۴.۳ قیمت دلار اختصاصی (`UserPricesDialog`)
- روی رکورد دسترسی (`ts_card_user_access.custom_unit_price_irt`) و فقط برای سکشن‌های غیر IRT.
- `POST card-user-price-update.php`: مقدار خالی → `NULL` (حذف override)؛ منفی → خطای ۴۰۰.
- تا وقتی مقدار ست نشود، کاربر در پنل خود قیمتی نمی‌بیند.

### ۴.۴ تب «پرداختی‌های کارت» (پرداخت ادمین به صاحب کارت)
- منبع: `card-admin-payments-list.php`؛ فیلتر کارت/وضعیت؛ KPI تعداد، جمع، جمع تأییدشده.
- عملیات: تأیید سریع، ویرایش (شامل تغییر منبع پرداخت)، حذف (اثر خزانه برگشت می‌خورد).
- **`card-debt-pay.php`:** رسید حداکثر ۱۰MB و فقط JPG/PNG/WEBP/PDF (بررسی MIME واقعی با `finfo`). اگر «از صندوق ترخیصان» و وضعیت `confirmed` باشد، ابتدا کفایت موجودی صندوق بررسی و سپس `ts_treasury_log('out', ...)` ثبت می‌شود؛ «پرداخت بیرونی» اثری روی صندوق ندارد.

### ۴.۵ تب «مدیریت کاربران»
- CRUD کاربران کارت + جستجو با نرمال‌سازی ارقام فارسی/عربی.
- قواعد فرم: نام و نام‌خانوادگی الزامی، نام کاربری `^[A-Za-z0-9_.\-]{3,64}$`، رمز حداقل ۶ کاراکتر و برابر با تکرار آن؛ در ویرایش، رمز خالی یعنی بدون تغییر.
- **رادیوی `require_payment_approval`:** `0` = پرداخت کاربر مستقیماً `confirmed`؛ `1` = پرداخت‌های او `pending` ثبت می‌شوند و تا تأیید ادمین روی حساب‌ها و خزانه اثر ندارند.
- دیالوگ‌های هر کاربر: گزارش پرداخت‌ها، گزارش کوتاژها، صورتحساب (`UserBillingDialog`).

### ۴.۶ تب «پرداخت‌های کاربران» (`AllPaymentsPanel`)
- فیلترها: جستجوی کاربر/کارت/مبلغ (با نرمال‌سازی ارقام و حروف فارسی)، کارت، وضعیت، بازهٔ تاریخ شمسی با `DatePicker`.
- خروجی‌ها: CSV، PDF (html2canvas + jsPDF، A4 افقی با صفحه‌بندی خودکار)، چاپ.
- ویرایش: مبلغ، وضعیت، یادداشت، و چک‌باکس «واریز به صندوق ترخیصان (در صورت تأیید)».
- **قانون همگام‌سازی خزانه:** در هر ویرایش ابتدا `ts_treasury_remove_source('user_payment', id)` اثر قبلی را پاک می‌کند و سپس اگر وضعیت `confirmed` و `to_treasury=1` بود رکورد `in` جدید درج می‌شود. یعنی خزانه همیشه بازسازی می‌شود و انباشتی/دوباره‌شمار نمی‌شود. وضعیت `pending` یا `rejected` هیچ اثری روی حساب‌ها و خزانه ندارد.

### ۴.۷ تب «کوتاژها»
جستجوی سراسری اقلام کوتاژ (`kotaj-items-search.php`) در سطح همهٔ کارت‌ها/کاربران: کارت، کاربر، عنوان سکشن، شماره/تاریخ کوتاژ، ارزش دلاری، نرخ، مبلغ تومانی و پیوست‌ها (با کلیک روی شماره کوتاژ، فایل‌های پیوست نمایش داده می‌شوند).

### ۴.۸ تب «بانک ترخیصان» (`TreasuryPanel`)
**فرمول موجودی (حسابداری نقدی صرف):**
```
total_in  = Σ ts_card_payments.amount_irt  (status=confirmed AND to_treasury=1)   + manual_adjust مثبت
total_out = Σ ts_card_admin_payments.amount_irt (status=confirmed AND from_treasury=1) + |manual_adjust منفی|
balance   = total_in − total_out
profit(هر کارت) = cash_in − cash_out       ← سود نقدی، نه سود تعهدی
```
- گرافیک گاوصندوق + ۴ KPI + نمودار روند ۳۰ روزه (recharts).
- دفتر کل با فیلتر کارت/جهت/بازه، خروجی CSV و PDF، و **صفحه‌بندی سمت کلاینت با دکمهٔ «ادامه» (۱۵ ردیف در هر بار)**.
- بلوک دیباگ «گزارش منبع داده صندوق» برای عیب‌یابی.

### ۴.۹ تب «گزارش‌گیری»
**زیرتب کارت (`reports-summary.php`):**
```
card_usd      = Σ entries[USD].amount
allocated_usd = Σ access.allocated (سکشن‌های USD)
used_usd      = Σ kotaj.total_value_usd
revenue_irt   = Σ (item.value_usd × item.unit_price_irt)        ← نرخ فروش به کاربر
cost_irt      = Σ (item.value_usd × entry.unit_price_irt)        ← نرخ خرید ادمین
profit_irt    = revenue_irt − cost_irt
admin_paid_irt = Σ admin_payments (confirmed)
admin_debt_remaining_irt = max(0, balance − admin_paid_irt)
remaining_usd = max(0, allocated_usd − used_usd)
```
خروجی شامل `totals`، `cards[]`، `top_users[]` (۲۰ نفر برتر بر اساس مصرف) و `recent[]` (۱۵ کوتاژ آخر).

**زیرتب کاربر:** KPIهای خرید/فروش/سود/بدهی/پرداختی/مانده + جدول قابل مرتب‌سازی و فیلتر وضعیت (دارای بدهی، بدون پرداخت، تسویه‌شده، اضافه‌پرداخت، سودده، زیان‌ده) با امکان باز کردن ریز کوتاژهای هر کاربر.

### ۴.۱۰ تب «بک‌آپ»
- **مخزن حافظه:** `backup-storage.php` → کل/آزاد/مصرف دیسک، حجم دیتابیس، حجم و تعداد بک‌آپ‌ها؛ نمایش گرافیکی با رنگ‌بندی (<۷۰٪ سبز، ۷۰–۹۰٪ زرد، ≥۹۰٪ قرمز). اگر هاست اجازهٔ خواندن دیسک ندهد، پیام مناسب نمایش داده می‌شود.
- **دانلود لحظه‌ای:** `backup-dump.php?format=sql|csv|xls` بدون ذخیره روی سرور.
- **بک‌آپ ذخیره‌شده:** `backup-list.php?auto=1` (اگر بیش از ۴۸ ساعت از آخرین بک‌آپ گذشته باشد خودکار می‌سازد)، `backup-create.php` (دستی)، `backup-download.php`، `backup-delete.php`. **هیچ حذف خودکاری انجام نمی‌شود؛ همهٔ نسخه‌ها تا حذف دستی باقی می‌مانند.** نمایش تدریجی ۱۵تایی.
- **کرون:** `backup-cron.php?key=<backup_cron_secret>`؛ منطق ۴۸ ساعت داخل اسکریپت است و اجرای مکرر بی‌اثر است.
- **بازگردانی:** بخش UI وجود دارد اما عمداً غیرفعال است (به دلایل امنیتی).

---

## ۵. `/TSCardUser` — پنل کاربر کارت

### ۵.۱ احراز هویت
`login.php` → `password_verify`؛ در خطا `usleep(300000)` و پیام یکسان برای جلوگیری از user enumeration. `me.php` وضعیت نشست (`loading | login | auth`) و `logout.php` خروج. همهٔ درخواست‌ها با `credentials: same-origin`.

### ۵.۲ لیست کارت‌ها (`my-cards.php`)
- کارت‌ها از `ts_card_user_access × ts_cards × ts_card_entries` واکشی می‌شوند؛ اگر تخصیصی نباشد پیام خالی نمایش داده می‌شود.
- برای هر سکشن: `remain = max(0, allocated − used)` و `remainTol = max(0, allocated × (1+tol/100) − used)`؛ نمایش «مانده با تلورانس» فقط وقتی بزرگ‌تر از مانده باشد.
- قیمت هر دلار فقط وقتی نمایش داده می‌شود که `custom_unit_price_irt` برای همان کاربر/سکشن ست شده باشد.
- **بج وضعیت:** `balance = payments(confirmed) − kotaj_toman`؛ منفی=بدهکار (قرمز)، مثبت=بستانکار (سبز)، صفر با فعالیت=تسویه (آبی)، بدون فعالیت=بدون بج.
- اکشن‌های هر کارت: افزودن کوتاژ، لیست کوتاژها، پرداخت (با نمایش بدهی)، صورتحساب. در هدر نیز «صورتحساب کلی» همهٔ کارت‌ها موجود است.

### ۵.۳ ثبت/ویرایش کوتاژ
- فقط روی سکشن‌های `USD` مجاز است.
- شمارهٔ کوتاژ فقط رقم، با فرمت خودکار `NNNNN-NNNNNNN`؛ **گمرک خروجی** فقط‌خواندنی و از ۵ رقم اول با جدول کدهای گمرکی استخراج می‌شود.
- تاریخ میلادی الزامی با نمایش معادل شمسی.
- اقلام (حداقل یک قلم): نام، ارزش دلاری `>0`، قیمت هر دلار.
- **پیش‌فرض نرخ:** فیلد قیمت با `custom_unit_price_irt` همان کاربر/سکشن پر می‌شود؛ با تغییر سکشن فقط فیلدهای خالی به‌روزرسانی می‌شوند و در ارسال، اگر فیلد خالی/صفر بود دوباره به همان نرخ مرجع fallback می‌شود. مقادیر کمتر/بیشتر از مرجع فقط رنگی هایلایت می‌شوند؛ **سرور فقط `price >= 0` را الزام می‌کند**، پس نرخ یک پیش‌فرض راهنماست نه محدودیت.
- **سقف و تلورانس (منبع حقیقت = سرور):**
```
allowedMax = allocated × (1 + tolerance_percent/100)
remain     = allowedMax − Σ(کوتاژهای قبلی همان entry، به‌جز رکورد در حال ویرایش)
اگر Σ ارزش اقلام − remain > 0.0001 → خطای ۴۰۰ با ذکر مانده و درصد تلورانس
```
کلاینت همین محاسبه را برای پیش‌نمایش زنده انجام می‌دهد و در حالت تخطی دکمهٔ ثبت را غیرفعال می‌کند.
- **ویرایش با تغییر سکشن:** `kotaj-update.php` تغییر `entry_id` را می‌پذیرد، دسترسی کاربر به سکشن مقصد را دوباره بررسی و سقف/تلورانس سکشن جدید را اعمال می‌کند؛ همهٔ محاسبات مرتبط با سکشن جدید بازمحاسبه می‌شوند.
- **پیوست‌ها:** چندتایی و اختیاری، JPG/PNG/WEBP/PDF، هر فایل ≤۱۰MB، بررسی MIME واقعی، ذخیره با نام تصادفی در `/uploads/kotaj/{user_id}/`. در ویرایش، فایل‌های نگه‌داشته‌شده از طریق `keep_attachments` تعیین می‌شوند.
- آپلود با `xhrUpload` (XHR برای گزارش پیشرفت، سقف نمایشی ۹۹٪ تا پاسخ سرور) و `UploadProgressBar`. پیش از ثبت، دیالوگ تأیید با خلاصهٔ دلاری/تومانی نمایش داده می‌شود.

### ۵.۴ لیست کوتاژها
فیلتر شماره، سکشن و بازهٔ تاریخ شمسی (سمت کلاینت)؛ هر ردیف قابل باز شدن با جدول اقلام و گالری پیوست‌ها؛ اکشن‌های دانلود PDF، ویرایش و حذف. حذف فقط با شرط مالکیت و بدون محدودیت زمانی انجام می‌شود.

### ۵.۵ ثبت پرداخت (`payment-create.php`)
- فیلدها: مبلغ (پیش‌فرض = بدهی فعلی، الزامی `>0`)، **تاریخ پرداخت شمسی** با تقویم جلالی (پیش‌فرض امروز)، توضیح اختیاری، و **فیش‌های واریزی چندتایی و کاملاً اختیاری** (ثبت بدون فیش مجاز است).
- وضعیت اولیه بر اساس `require_payment_approval` کاربر: `pending` یا `confirmed`.
- فقط در حالت `confirmed` فراخوانی `ts_treasury_log('in', ...)` انجام می‌شود؛ پرداخت‌های `pending` و `rejected` روی موجودی، بدهی و خزانه اثری ندارند و پس از تأیید ادمین اعمال می‌شوند.
- بازخورد کاربر با پیام متفاوت برای حالت در انتظار تأیید.

### ۵.۶ صورتحساب
- تک‌کارت و کلی (همهٔ کارت‌ها) با تایم‌لاین ادغام‌شدهٔ کوتاژ + پرداخت مرتب بر اساس تاریخ، برچسب وضعیت رنگی، لینک فیش‌ها، خروجی PDF و چاپ.
- تولید PDF کوتاژ (`kotaj-pdf.ts`): رندر HTML راست‌چین با `html2canvas` (scale=2) و درج در `jsPDF` A4 با برش دستی برای چندصفحه‌ای شدن؛ خروجی `kotaj-{number}.pdf`؛ ورودی‌های کاربر برای جلوگیری از XSS escape می‌شوند.

---

## ۶. قواعد کسب‌وکار مشترک (خلاصهٔ نهایی)

1. سهمیهٔ قابل مصرف هر سکشن = `allocated × (1 + tolerance_percent/100)`؛ اعتبارسنجی نهایی همیشه سمت سرور.
2. تلورانس فقط نمایشی/سقفی است و موجودی ذخیره‌شده را تغییر نمی‌دهد، مگر با «اتمام کارت».
3. «اتمام کارت» مقدار واقعی مصرف‌شده را در ستون‌های نمایشی snapshot می‌کند و روی وضعیت مانده و دیالوگ پرداخت اثر می‌گذارد؛ قابل بازگشت است.
4. نرخ هر دلار در سه سطح وجود دارد: نرخ خرید سکشن (`entry.unit_price_irt`)، نرخ اختصاصی کاربر (`access.custom_unit_price_irt` = پیش‌فرض فرم)، و نرخ ثبت‌شده در قلم کوتاژ (اسنپ‌شات نهایی که مبنای مالی است).
5. بدهی کاربر = هزینهٔ کوتاژها − پرداخت‌های **تأییدشده**.
6. تأیید دستی پرداخت در سطح هر کاربر قابل تنظیم است؛ پرداخت‌های `pending`/`rejected` در هیچ محاسبه‌ای لحاظ نمی‌شوند.
7. خزانه فقط با پرداخت‌های `confirmed` و دارای فلگ (`to_treasury` / `from_treasury`) جابه‌جا می‌شود و در هر ویرایش بازسازی می‌گردد.
8. سکشن‌ها همیشه با `entry_id` یکتا شناسایی می‌شوند تا سکشن‌های هم‌نام یکدیگر را خراب نکنند.
9. همهٔ آپلودها: JPG/PNG/WEBP/PDF، ≤۱۰MB، بررسی MIME واقعی، نام فایل تصادفی.
10. بک‌آپ خودکار هر ۴۸ ساعت، بدون حذف خودکار نسخه‌های قدیمی.

---

## ۷. موارد UNVERIFIED / نیازمند بررسی تکمیلی

- بدنهٔ دقیق برخی endpointها فقط از روی مصرف کلاینت استنتاج شده است: `card-update.php`، `card-user-update.php`، `card-user-kotaj-report.php`، `card-user-payments.php`، `payments-list-all.php`، `reports-users-summary.php`، `treasury-ledger.php`، `kotaj-items-search.php`.
- پاک‌سازی فیزیکی فایل‌های پیوستِ حذف‌شده (orphan) روی دیسک در هیچ endpointی دیده نشد.
- فیلتر وضعیت در `leads.php` سمت کلاینت است و با محدودیت واکشی ممکن است نتایج ناقص بدهد.
- در `BillingDialog` کاربر، «مجموع پرداختی» بالای دیالوگ فقط `confirmed` است اما جمع پایین جدول همهٔ وضعیت‌ها را جمع می‌زند (ناسازگاری بالقوهٔ نمایشی).
- بخش «بازگردانی نسخه پشتیبان» صرفاً Placeholder است.

---

*این سند بر اساس Implementation واقعی پروژه تولید شده است.*
